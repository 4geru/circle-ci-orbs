version: 2.1

orbs:
  awesome-emoji: a4geru/awesome-emoji@1.0.0

jobs:
  build:
    docker:
    - image: bash
    steps:
      - run: "echo 'testing building...'"
      - awesome-emoji/echo:
          emoji: beer
          time: 10

workflows:
  main:
    jobs:
      - build
